---
phase: 01-foundation-pipeline-motion-system
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/motion/Reveal.tsx
  - components/motion/ScrollParallax.tsx
  - components/motion/SmoothScroll.tsx
  - components/ui/SectionShell.tsx
  - components/ui/CTAButton.tsx
  - components/ui/GradientText.tsx
  - components/motion/motion.test.tsx
autonomous: true
requirements: [MOTION-02]
must_haves:
  truths:
    - "Reveal renders its children with an entrance animation only when motion is allowed; under reduced motion it renders children fully visible with NO transition (reduced motion is the base path)"
    - "ScrollParallax applies a transform-only (translateY) parallax when motion is allowed and applies NO transform under reduced motion"
    - "SmoothScroll (Lenis) is fully disabled under reduced motion — no smooth-scroll hijack, native scroll preserved"
    - "Every primitive gates through useReducedMotionSafe from Plan 01, so the reduced-motion contract lives in one place and is the default before hydration"
    - "UI primitives (SectionShell, CTAButton, GradientText) render against the cinematic sci-fi tokens and the CTA is a real <a>/<button> (keyboard reachable)"
  artifacts:
    - path: "components/motion/Reveal.tsx"
      provides: "whileInView fade/slide wrapper, reduced-motion-gated, transform/opacity only"
      contains: "useReducedMotionSafe"
    - path: "components/motion/ScrollParallax.tsx"
      provides: "Scroll-linked translateY parallax, reduced-motion-gated"
      contains: "useReducedMotionSafe"
    - path: "components/motion/SmoothScroll.tsx"
      provides: "Lenis smooth-scroll driven off GSAP ticker, disabled under reduced motion"
      contains: "lenis"
    - path: "components/ui/CTAButton.tsx"
      provides: "Accessible primary CTA primitive (real anchor/button, focus-visible ring)"
      contains: "focus"
    - path: "components/motion/motion.test.tsx"
      provides: "Tests proving reduced-motion is the base path for the primitives"
      contains: "reduced"
  key_links:
    - from: "components/motion/Reveal.tsx"
      to: "components/motion/useReducedMotionSafe.ts"
      via: "import + early-return static children when reduced"
      pattern: "useReducedMotionSafe"
    - from: "components/motion/ScrollParallax.tsx"
      to: "motion/react"
      via: "useScroll/useTransform on transform only"
      pattern: "useScroll|useTransform"
    - from: "components/motion/SmoothScroll.tsx"
      to: "gsap ticker"
      via: "drive Lenis RAF off gsap.ticker, ScrollTrigger.update on scroll"
      pattern: "ticker|raf"
---

<objective>
Build the reduced-motion-first motion-layer primitives and the shared UI primitives that every Phase 2+ section will consume — so sections never reimplement animation or reduced-motion handling. Reveal (entrance), ScrollParallax (depth), and SmoothScroll (Lenis scroll spine) all gate through the single useReducedMotionSafe hook from Plan 01, making the static reduced path the BASE case and full motion the enhancement. Animations touch only transform/opacity (no layout-shifting properties).

Purpose: MOTION-02 — all motion is reduced/disabled under prefers-reduced-motion and the page stays usable without it, established structurally so it never has to be retrofitted. Provides the consumable motion + UI layer the architecture mandates.
Output: components/motion/{Reveal,ScrollParallax,SmoothScroll}.tsx, components/ui/{SectionShell,CTAButton,GradientText}.tsx, and a test proving the reduced-motion base path.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts produced by Plan 01-01 that this plan consumes. Use directly; do not re-explore. -->

components/motion/useReducedMotionSafe.ts (from Plan 01):
```ts
export function useReducedMotionSafe(): boolean; // true => suppress motion (default before hydration)
```

components/motion/MotionProvider.tsx (from Plan 01): mounts <MotionConfig reducedMotion="user"> at the root. Primitives still gate explicitly for transform-only fallbacks and for non-motion libs (Lenis/GSAP).

styles/tokens.ts (from Plan 01):
```ts
export const tokens: {
  bg: string; fg: string; accent: string; accent2: string; accent3: string;
  easeOut: readonly [number, number, number, number]; // [0.16,1,0.3,1]
  revealDuration: number; // 0.6
  parallaxRange: number;  // 80 (px)
};
```

CSS tokens available (from styles/tokens.css): --ufa-bg, --ufa-fg, --ufa-muted, --ufa-accent, --ufa-accent-iridescent, --ufa-type-display, --ufa-space-section, --ufa-radius, --ufa-ease-out, --ufa-glow.

Package import paths (verified): motion -> `import { motion, useScroll, useTransform } from "motion/react"`; lenis -> `import Lenis from "lenis"`; gsap -> `import { gsap } from "gsap"`. All primitives are "use client".
ANTI-PATTERN (research/PITFALLS.md): never animate top/left/width/height/margin; only transform/opacity/filter. Init scroll-linked motion in effects after mount.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reveal + ScrollParallax primitives (reduced-motion is the base path)</name>
  <files>components/motion/Reveal.tsx, components/motion/ScrollParallax.tsx</files>
  <read_first>
    - components/motion/useReducedMotionSafe.ts
    - styles/tokens.ts
    - .planning/research/ARCHITECTURE.md (Pattern 2: Motion as a Consumed Layer)
    - .planning/research/PITFALLS.md (Anti-Pattern 2 + Pitfall 4/5)
  </read_first>
  <behavior>
    - Reveal, when reduced motion is active: renders children directly, fully visible (opacity 1, no transform), and emits NO motion transition (the static base path).
    - Reveal, when motion allowed: wraps children in motion.div with initial {opacity:0, y:24} -> whileInView {opacity:1, y:0}, viewport once, transition duration tokens.revealDuration, ease tokens.easeOut. Only transform/opacity animate.
    - ScrollParallax, when reduced motion active: renders children with no transform / no scroll listener.
    - ScrollParallax, when motion allowed: applies a translateY parallax derived from useScroll progress over tokens.parallaxRange px (transform only).
  </behavior>
  <action>
    Create `components/motion/Reveal.tsx` ("use client"):
    - Props: { children: React.ReactNode; y?: number; delay?: number }, default y = 24.
    - `const reduced = useReducedMotionSafe(); if (reduced) return <>{children}</>;`
    - Else return motion.div with initial {opacity:0,y}, whileInView {opacity:1,y:0}, viewport {once:true, margin:"0px 0px -15% 0px"}, transition {duration: tokens.revealDuration, delay, ease: tokens.easeOut}.

    Create `components/motion/ScrollParallax.tsx` ("use client"):
    - Props: { children: React.ReactNode; range?: number } default range = tokens.parallaxRange.
    - `const reduced = useReducedMotionSafe(); if (reduced) return <div>{children}</div>;`
    - Else use a ref + `const { scrollYProgress } = useScroll({ target: ref, offset: ["start end","end start"] });` and `const y = useTransform(scrollYProgress, [0,1], [range, -range]);` then `<motion.div ref={ref} style={{ y }}>{children}</motion.div>`. Transform only — never animate top/height.
    Import tokens from "@/styles/tokens". Both files must call useReducedMotionSafe so the gate is centralized.
  </action>
  <verify>
    <automated>cd /Users/prakshaljain/OS-1/ufa && grep -q "useReducedMotionSafe" components/motion/Reveal.tsx && grep -q "useReducedMotionSafe" components/motion/ScrollParallax.tsx && grep -q "whileInView" components/motion/Reveal.tsx && grep -Eq "useScroll|useTransform" components/motion/ScrollParallax.tsx && ! grep -Eq "(^|[^-])(top|left|width|height|margin)\s*:" components/motion/ScrollParallax.tsx && npm run typecheck && echo PRIMITIVES-OK</automated>
  </verify>
  <acceptance_criteria>
    - components/motion/Reveal.tsx contains "use client", "useReducedMotionSafe", and "whileInView"
    - components/motion/Reveal.tsx early-returns plain children when reduced (grep: "return <>{children}</>" or equivalent fragment return)
    - components/motion/ScrollParallax.tsx contains "useScroll" and "useTransform" and "useReducedMotionSafe"
    - ScrollParallax does NOT reference layout-shifting CSS props (no top/left/width/height/margin in a style object)
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Reveal and ScrollParallax exist, animate only transform/opacity, and short-circuit to a fully-visible static render under reduced motion via the centralized hook.</done>
</task>

<task type="auto">
  <name>Task 2: SmoothScroll (Lenis on GSAP ticker), disabled under reduced motion</name>
  <files>components/motion/SmoothScroll.tsx</files>
  <read_first>
    - components/motion/useReducedMotionSafe.ts
    - .planning/research/STACK.md (lenis row + Lenis+GSAP ticker pattern)
    - .planning/research/PITFALLS.md (Pitfall 5: prefers-reduced-motion; Performance Traps: scroll-jack)
  </read_first>
  <action>
    Create `components/motion/SmoothScroll.tsx` ("use client") — the scroll-spine wiring, reduced-motion-gated:
    - `const reduced = useReducedMotionSafe();`
    - `useEffect(() => { if (reduced) return; ... }, [reduced]);` — when reduced, do nothing (native scroll, no Lenis): this is the base path.
    - When allowed: instantiate `const lenis = new Lenis({ lerp: 0.1, smoothWheel: true });`. Register gsap: `gsap.registerPlugin(ScrollTrigger)` (import ScrollTrigger from "gsap/ScrollTrigger"). Drive Lenis off the GSAP ticker: `gsap.ticker.add((time)=>lenis.raf(time*1000)); gsap.ticker.lagSmoothing(0);` and `lenis.on("scroll", ScrollTrigger.update);`.
    - Cleanup in the effect return: remove the ticker callback and call `lenis.destroy()`.
    - Component renders `<>{children}</>` (it is a behavior wrapper, no DOM box). Props: { children: React.ReactNode }.
    Do NOT scroll-jack or hijack scroll position; Lenis only eases native scroll and is entirely absent under reduced motion. Guard all GSAP/Lenis usage inside useEffect (never at module top — keeps it a client leaf, avoids SSR/hydration issues per Pitfall 4).
  </action>
  <verify>
    <automated>cd /Users/prakshaljain/OS-1/ufa && grep -qi "lenis" components/motion/SmoothScroll.tsx && grep -q "useReducedMotionSafe" components/motion/SmoothScroll.tsx && grep -Eq "ticker|raf" components/motion/SmoothScroll.tsx && grep -q "destroy" components/motion/SmoothScroll.tsx && grep -q "if (reduced) return" components/motion/SmoothScroll.tsx && npm run typecheck && echo SMOOTHSCROLL-OK</automated>
  </verify>
  <acceptance_criteria>
    - components/motion/SmoothScroll.tsx contains "use client", "Lenis", "useReducedMotionSafe"
    - Lenis is instantiated inside a useEffect (not at module top level)
    - The effect early-returns when reduced (grep: "if (reduced) return")
    - Lenis RAF is driven off gsap.ticker (grep matches "ticker" and "raf")
    - Cleanup calls lenis.destroy()
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>SmoothScroll eases native scroll via Lenis on the GSAP ticker when motion is allowed and is completely inert (native scroll) under reduced motion, with proper effect-scoped init and teardown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UI primitives (SectionShell, CTAButton, GradientText) + reduced-motion base-path test</name>
  <files>components/ui/SectionShell.tsx, components/ui/GradientText.tsx, components/ui/CTAButton.tsx, components/motion/motion.test.tsx, components/motion/reduced-motion.smoke.mjs</files>
  <read_first>
    - styles/tokens.css
    - components/motion/Reveal.tsx
    - .planning/research/PITFALLS.md (UX Pitfalls: low contrast on gradients, keyboard focus, CTA reachability)
  </read_first>
  <behavior>
    - With prefers-reduced-motion (mocked so useReducedMotionSafe returns true): Reveal renders its child text into the DOM immediately and visibly, and does NOT emit motion-animation props — proving the reduced path is the base case.
    - CTAButton renders a real focusable interactive element (an <a> when href given, else a <button>) so it is keyboard reachable.
  </behavior>
  <action>
    Create `components/ui/SectionShell.tsx` (server component, no "use client"): a <section> applying consistent rhythm via tokens — padding-block var(--ufa-space-section), max-width container centered, horizontal padding clamp(1.25rem,5vw,3rem). Props { children, id?, className? }. Use the --ufa-* CSS vars (inline style or Tailwind utilities mapped from @theme).

    Create `components/ui/GradientText.tsx` (server component): wraps text in a span using the iridescent gradient as a clipped text fill — `background: var(--ufa-accent-iridescent); -webkit-background-clip:text; background-clip:text; color:transparent;`. Props { children, className? }. NOTE for accessibility: gradient is decorative fill on solid text; default fallback color must be --ufa-fg if background-clip unsupported.

    Create `components/ui/CTAButton.tsx` (can be a server component): a real interactive element — render `<a href={href}>` when `href` prop present, otherwise `<button type="button">`. Props { children, href?, onClick?, className? }. Styling via tokens: background var(--ufa-accent), color var(--ufa-bg), border-radius var(--ufa-radius), padding 0.9rem 1.6rem, and a visible focus ring: `:focus-visible` outline using --ufa-accent-3 (define this as a class in the component or a styled span; ensure a focus-visible rule exists). It MUST be keyboard focusable and have an accessible name from children.

    Create `components/motion/motion.test.tsx` proving the reduced-motion base path. Since no test runner is configured, install Vitest + React Testing Library as devDeps is OUT OF SCOPE for this plan (no package.json changes here). Instead write a lightweight assertion script runnable with the Node test runner already available in Node 22: create the file as `components/motion/motion.test.mjs` using `node:test` + `node:assert` that imports the COMPILED behavior indirectly is not feasible for TSX. RESOLUTION: write `components/motion/motion.test.tsx` as a documented React Testing Library spec (describe/it) AND add a runnable smoke check: a `node:test` file `components/motion/reduced-motion.smoke.mjs` that statically asserts each primitive source early-returns under `reduced` (reads the .tsx source text and asserts the presence of the reduced-branch guard for Reveal, ScrollParallax, SmoothScroll). This gives an automated, dependency-free gate that the reduced-motion base path is wired in all three primitives. Reference in the test file header that full RTL tests run once a test runner is added in a later phase.
  </action>
  <verify>
    <automated>cd /Users/prakshaljain/OS-1/ufa && test -f components/ui/SectionShell.tsx && test -f components/ui/GradientText.tsx && test -f components/ui/CTAButton.tsx && grep -Eq "href|button" components/ui/CTAButton.tsx && grep -q "focus" components/ui/CTAButton.tsx && grep -q "background-clip" components/ui/GradientText.tsx && node --test components/motion/reduced-motion.smoke.mjs && npm run typecheck && npm run build && node scripts/verify-build.mjs 2>/dev/null; npm run build && test -f out/CNAME && echo UI-AND-TEST-OK</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/SectionShell.tsx, GradientText.tsx, CTAButton.tsx all exist
    - CTAButton.tsx renders <a> when href present else <button>, and contains a focus/focus-visible rule (grep "focus")
    - GradientText.tsx contains "background-clip" and a fallback color of --ufa-fg
    - components/motion/reduced-motion.smoke.mjs runs under `node --test` and asserts each of Reveal/ScrollParallax/SmoothScroll contains a reduced-motion early-return branch; the run exits 0
    - npm run typecheck exits 0
    - npm run build exits 0 and out/CNAME still present (no regression to the static export)
  </acceptance_criteria>
  <done>Shared UI primitives render against cinematic tokens with an accessible, keyboard-reachable CTA and AA-safe gradient text fallback, and an automated dependency-free test proves the reduced-motion base path is wired into all three motion primitives.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0 with all new components.
- `node --test components/motion/reduced-motion.smoke.mjs` exits 0 (reduced-motion base path verified in all primitives).
- `npm run build` exits 0; out/CNAME + out/.nojekyll still present (no static-export regression).
- ScrollParallax/Reveal contain no layout-shifting animated properties (transform/opacity only).
- SmoothScroll inits Lenis only inside useEffect and only when motion is allowed.
</verification>

<success_criteria>
- MOTION-02: Reveal, ScrollParallax, and SmoothScroll all serve a fully static / native base path under prefers-reduced-motion via the centralized useReducedMotionSafe gate — reduced motion is the default, full motion is the enhancement, verifiable in isolation.
- Sections can consume motion + UI primitives without reimplementing animation or reduced-motion handling.
- The CTA primitive is a real keyboard-reachable element with a visible focus ring; gradient text degrades to a legible solid color.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pipeline-motion-system/01-03-SUMMARY.md`
</output>
