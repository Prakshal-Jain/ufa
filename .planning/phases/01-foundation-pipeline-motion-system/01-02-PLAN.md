---
phase: 01-foundation-pipeline-motion-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/verify-build.mjs
  - .github/workflows/deploy.yml
  - app/opengraph-image.tsx
autonomous: true
requirements: [SITE-02, SITE-03, SITE-04]
must_haves:
  truths:
    - "A build-output guard fails (non-zero exit) if out/CNAME or out/.nojekyll is missing, or if next.config.mjs contains a non-empty basePath/assetPrefix"
    - "The guard runs in CI after the build so a regression that drops GH Pages files or sets basePath can never deploy silently"
    - "Pushing to main still auto-builds and deploys via the existing GitHub Actions pipeline (workflow remains valid and hardened, not rewritten)"
    - "Sharing the deployed link renders a static Open Graph image (out/opengraph-image* exists after build) plus title and description"
  artifacts:
    - path: "scripts/verify-build.mjs"
      provides: "Asset-presence + basePath guard run against out/ and next.config.mjs"
      contains: "out/.nojekyll"
    - path: ".github/workflows/deploy.yml"
      provides: "Hardened deploy pipeline: typecheck + build + verify guard before upload"
      contains: "verify:build"
    - path: "app/opengraph-image.tsx"
      provides: "Static OG image generated at build time into out/"
      contains: "force-static"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "scripts/verify-build.mjs"
      via: "npm run verify:build step after npm run build, before upload-pages-artifact"
      pattern: "verify:build"
    - from: "app/opengraph-image.tsx"
      to: "out/opengraph-image"
      via: "Next metadata file convention prerendered under output:export"
      pattern: "ImageResponse|force-static"
---

<objective>
Harden the (already-working) delivery pipeline so the #1 catastrophic risk — a dead/broken link sent to an investor — cannot ship silently, and add the static Open Graph image so shared links preview well. Add a build-output guard that fails the build if CNAME/.nojekyll are dropped or basePath is set, wire it into the existing GitHub Actions workflow, and add a static, build-time-prerendered OG image.

Purpose: SITE-02 (assets resolve from apex root; CNAME/.nojekyll preserved), SITE-03 (push-to-main auto-deploy stays green), SITE-04 (static OG image preview). This is infra hardening — extend the existing pipeline, do NOT re-solve hosting.
Output: scripts/verify-build.mjs guard, a hardened deploy.yml that runs typecheck + build + guard before publishing, and app/opengraph-image.tsx producing a static OG card.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Read these before editing. -->

Existing .github/workflows/deploy.yml: build job runs checkout -> setup-node@v4 (node 22, cache npm) -> npm ci -> npm run build -> upload-pages-artifact (path ./out); deploy job uses deploy-pages@v4. Do NOT change the trigger (push to main + workflow_dispatch), permissions, concurrency, or the deploy job. Only ADD guard steps in the build job between `npm run build` and the upload step, plus a typecheck step before build.

Plan 01-01 adds npm scripts "typecheck" (tsc --noEmit) and "verify:build" (node scripts/verify-build.mjs) — rely on them; do NOT modify package.json here.

next.config.mjs: output:"export", images.unoptimized:true, trailingSlash:true, NO basePath/assetPrefix. The guard must assert basePath/assetPrefix stay empty.

OG image under static export: research/PITFALLS.md "Integration Gotchas" warns dynamic opengraph-image routes don't statically generate. A NON-dynamic app/opengraph-image.tsx with `export const dynamic = "force-static"` IS prerendered at build into out/. Verify the emitted file exists in out/ as the acceptance gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the build-output guard (CNAME/.nojekyll presence + empty basePath)</name>
  <files>scripts/verify-build.mjs</files>
  <read_first>
    - next.config.mjs
    - public/CNAME
    - .planning/research/PITFALLS.md (Pitfall 1 + "Looks Done But Isn't" checklist)
  </read_first>
  <action>
    Create `scripts/verify-build.mjs` (ESM, Node 22, zero external deps; use only node:fs, node:path, node:url). It must, in order:
    1. Resolve the project root relative to the script's own location (import.meta.url).
    2. Assert `out/CNAME` exists AND its trimmed content === "ufa.foundation". Fail (console.error + process.exit(1)) otherwise.
    3. Assert `out/.nojekyll` exists. Fail otherwise.
    4. Read `next.config.mjs` as UTF-8 text and assert there is NO non-empty `basePath` or `assetPrefix`. Logic: match /basePath\s*:\s*(["'])(.*?)\1/ and /assetPrefix\s*:\s*(["'])(.*?)\1/ ; if a match exists and capture group 2 is a non-empty string, FAIL. An absent key passes; an empty-string value ("") passes.
    5. Assert `out/index.html` exists.
    6. Assert at least one file matching /\.css$/ exists somewhere under `out/_next/static` (recursive scan), proving Tailwind/CSS shipped.
    7. On all-pass: print "verify-build: OK" and exit 0; otherwise exit 1.
    Each failing check must print exactly which check failed and the fix hint (e.g. "out/.nojekyll missing — ensure public/.nojekyll exists and is copied by the build").
  </action>
  <verify>
    <automated>cd /Users/prakshaljain/OS-1/ufa && npm run build && node scripts/verify-build.mjs && echo GUARD-PASS && mv out/.nojekyll out/.nojekyll.bak && ( node scripts/verify-build.mjs ; test $? -ne 0 && echo GUARD-CATCHES-MISSING ) ; mv out/.nojekyll.bak out/.nojekyll</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-build.mjs exists and references "out/CNAME", "out/.nojekyll", "basePath", "assetPrefix"
    - scripts/verify-build.mjs imports only from "node:fs"/"node:path"/"node:url" (no external require/import)
    - Running the guard against a correct out/ prints "verify-build: OK" and exits 0
    - Temporarily removing out/.nojekyll makes the guard exit non-zero (GUARD-CATCHES-MISSING printed)
    - The guard fails if next.config.mjs gains a non-empty basePath
  </acceptance_criteria>
  <done>A dependency-free guard fails the build whenever CNAME/.nojekyll are missing or basePath/assetPrefix become non-empty, and passes on the current correct output.</done>
</task>

<task type="auto">
  <name>Task 2: Add a static, build-time Open Graph image</name>
  <files>app/opengraph-image.tsx</files>
  <read_first>
    - app/layout.tsx
    - .planning/research/PITFALLS.md (Integration Gotchas: Next.js OG image)
    - .planning/PROJECT.md (Design constraint: cinematic sci-fi)
  </read_first>
  <action>
    Create `app/opengraph-image.tsx` using Next's metadata file convention with `next/og` ImageResponse, forced static so it prerenders at build under output:"export":
    ```tsx
    import { ImageResponse } from "next/og";
    export const dynamic = "force-static";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";
    export const alt = "Ultimate Agent Fight";
    export default function OG() {
      return new ImageResponse(
        (
          <div style={{ /* cinematic sci-fi: deep bg + iridescent accent */ }}>
            {/* UFA wordmark + one-line value: "AI agents interrogate and fight for credits & money" */}
          </div>
        ),
        size
      );
    }
    ```
    Concrete design values (inline styles only — next/og supports a subset of CSS, no external fonts required, use system/sans):
    - Root div: width 1200, height 630, display flex, flexDirection column, justifyContent center, padding 80px, background "linear-gradient(120deg, #05060a 0%, #0b0d16 60%, #1a0f2e 100%)", color "#f4f6ff".
    - Wordmark "UFA" fontSize 200, fontWeight 800, letterSpacing 8, with the iridescent feel via color "#a77aff".
    - Subline "ULTIMATE AGENT FIGHT" fontSize 40, letterSpacing 12, color "#7aa2ff", textTransform uppercase.
    - Value line "AI agents interrogate and fight to win credits & money." fontSize 34, color "#8a90a6", marginTop 24.
    Do NOT add a dynamic route or use request-time data. Do NOT modify app/layout.tsx (its metadataBase/title/description already feed the share card from Plan 01).
  </action>
  <verify>
    <automated>cd /Users/prakshaljain/OS-1/ufa && grep -q "force-static" app/opengraph-image.tsx && grep -q "ImageResponse" app/opengraph-image.tsx && npm run build && (ls out/opengraph-image* >/dev/null 2>&1 || find out -name 'opengraph-image*' | grep -q . ) && echo OG-IMAGE-EMITTED</automated>
  </verify>
  <acceptance_criteria>
    - app/opengraph-image.tsx exists and contains "ImageResponse" and `export const dynamic = "force-static"`
    - app/opengraph-image.tsx exports size = { width: 1200, height: 630 } and contentType "image/png"
    - app/opengraph-image.tsx contains the value line text "interrogate" and "credits"
    - After npm run build, an opengraph-image asset exists under out/ (out/opengraph-image* or a hashed variant found by find)
    - app/layout.tsx is unchanged by this task
  </acceptance_criteria>
  <done>A static cinematic OG card is prerendered into out/ at build time, giving shared links a correct image preview without any server runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the GitHub Actions deploy workflow with typecheck + build + guard gates</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml
    - package.json (scripts added by Plan 01-01: typecheck, verify:build)
    - .planning/research/PITFALLS.md (No CI lint/typecheck before deploy)
  </read_first>
  <action>
    Edit `.github/workflows/deploy.yml` build job ONLY. Keep the existing trigger (push main + workflow_dispatch), permissions, concurrency, and the entire deploy job exactly as-is. In the build job steps, insert:
    - After `- run: npm ci`, add `- run: npm run typecheck` (fails CI on type errors before building).
    - Keep `- run: npm run build`.
    - Immediately AFTER `- run: npm run build` and BEFORE the `upload-pages-artifact` step, add `- run: npm run verify:build` (the guard: fails if CNAME/.nojekyll missing or basePath set).
    Leave `actions/checkout@v4`, `actions/setup-node@v4` (node-version 22, cache npm), `upload-pages-artifact@v3` (path ./out), and `deploy-pages@v4` unchanged. The result must remain valid YAML.
  </action>
  <verify>
    <automated>cd /Users/prakshaljain/OS-1/ufa && grep -q "npm run typecheck" .github/workflows/deploy.yml && grep -q "npm run verify:build" .github/workflows/deploy.yml && grep -q "upload-pages-artifact@v3" .github/workflows/deploy.yml && grep -q "deploy-pages@v4" .github/workflows/deploy.yml && node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/deploy.yml','utf8');const b=y.indexOf('npm run build');const v=y.indexOf('npm run verify:build');const u=y.indexOf('upload-pages-artifact');if(!(b<v&&v<u))throw new Error('verify:build must sit between build and upload');console.log('ORDER-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy.yml contains "npm run typecheck" before "npm run build"
    - .github/workflows/deploy.yml contains "npm run verify:build" positioned AFTER "npm run build" and BEFORE "upload-pages-artifact"
    - The workflow still contains the unchanged trigger (push branches main + workflow_dispatch), permissions (pages: write, id-token: write), concurrency group pages, upload-pages-artifact@v3 (path ./out), and deploy-pages@v4
    - The file is valid YAML (node order-check script prints ORDER-OK)
  </acceptance_criteria>
  <done>CI typechecks then builds then runs the asset/basePath guard before uploading, so a push to main that would break the apex link fails the pipeline instead of deploying silently — while the existing hosting mechanism is otherwise untouched.</done>
</task>

</tasks>

<verification>
- `npm run build && node scripts/verify-build.mjs` exits 0; removing out/.nojekyll makes the guard exit non-zero.
- An opengraph-image asset is emitted under out/ after build.
- deploy.yml is valid YAML; verify:build runs between build and upload; deploy job unchanged.
- Adding a fake `basePath: "/x"` to next.config.mjs makes the guard fail (spot-check, then revert).
</verification>

<success_criteria>
- SITE-02: CNAME + .nojekyll presence and empty basePath/assetPrefix are CI-enforced; build fails if any regress.
- SITE-03: push-to-main pipeline remains green and now gated by typecheck + build + guard before deploy.
- SITE-04: a static OG image is prerendered into out/ for correct link previews.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pipeline-motion-system/01-02-SUMMARY.md`
</output>
