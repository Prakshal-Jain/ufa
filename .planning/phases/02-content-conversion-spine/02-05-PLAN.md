---
phase: 02-content-conversion-spine
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - components/sections/HeroHeader.tsx
  - components/ui/Nav.tsx
  - app/page.tsx
  - app/layout.tsx
  - app/globals.css
autonomous: true
requirements: [MOTION-01, MOTION-03]
must_haves:
  truths:
    - "The page is the full scrolling landing page (hero header -> Concept -> HowItWorks -> MatchShowcase -> PrizePool -> Standings -> SponsorValue -> Traction -> Lineage -> SponsorCTA -> Footer), replacing the Coming Soon placeholder"
    - "A simple, strong static hero header at the top states what UFA is and surfaces the sponsor CTA within the first screen"
    - "Scroll-driven cinematic motion animates the sections via the Phase-1 primitives (no reimplemented animation), wrapped in SmoothScroll (Lenis), reduced-motion still the base case"
    - "The page reads fully and converts with motion stripped; share metadata describes the real site"
  artifacts:
    - path: "components/sections/HeroHeader.tsx"
      provides: "Static first-screen hero with H1 value statement + sponsor CTA (Phase 3/4 will upgrade)"
      contains: "credits"
      min_lines: 30
    - path: "app/page.tsx"
      provides: "Composition root ordering all sections + SmoothScroll wrap"
      contains: "HeroHeader"
      min_lines: 25
    - path: "app/layout.tsx"
      provides: "Updated real-site metadata (title/description/OG)"
      contains: "Ultimate Agent Fight"
  key_links:
    - from: "app/page.tsx"
      to: "all section components + SmoothScroll"
      via: "import + render in order"
      pattern: "SmoothScroll|HeroHeader|SponsorCTA"
    - from: "app/page.tsx"
      to: "components/motion/SmoothScroll.tsx"
      via: "wrap page in Lenis smooth scroll (reduced-motion gated)"
      pattern: "SmoothScroll"
---

<objective>
Assemble the complete scrolling landing page: build a simple, strong STATIC hero header (Phase 3/4 will upgrade it to the cinematic poster + WebGL beat), then compose every Wave-2 section in the locked conversion-optimized order in app/page.tsx, wrap the page in SmoothScroll (Lenis, reduced-motion-gated), and update layout metadata + the hero header CSS so the page is a complete, sendable site. This is the sequenced assembly plan - it owns the shared files (page/layout/globals) to avoid contention with the parallel Wave-2 plans.

Purpose: Turn the independently-built sections into the conversion spine. The first screen must state what UFA is and surface the sponsor CTA (substance-over-style), and the whole page must read with motion stripped.
Output: HeroHeader.tsx, Nav.tsx, updated app/page.tsx, app/layout.tsx, app/globals.css.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Sections built in Plans 02/03/04 (confirm exact export names against those files). Each is exported as a named function from components/sections/<Name>.tsx: -->
  Concept, HowItWorks, Lineage           (Plan 02)
  MatchShowcase, PrizePool, Standings    (Plan 03)
  SponsorValue, Traction, SponsorCTA, Footer (Plan 04)
<!-- Phase-1 primitives to REUSE: -->
From components/motion/SmoothScroll.tsx ("use client"): function SmoothScroll({ children }) - mounts Lenis on the GSAP ticker, early-returns (native scroll) under reduced motion. Behavior wrapper, no DOM box. Mount ONCE near the page root.
From components/ui/SectionShell.tsx, GradientText.tsx, CTAButton.tsx; components/motion/Reveal.tsx, ScrollParallax.tsx.
Data: data/site.ts -> site.{anchors, fullName, brand, tagline}; data/concept.ts -> conceptCopy (for the hero H1/value line). Path alias "@/*".

Current files to MODIFY (read them first):
  app/page.tsx  - currently the Coming Soon placeholder (.screen/.content/.wordmark). REPLACE its content.
  app/layout.tsx - MotionProvider already mounted; metadata currently says "coming soon". UPDATE metadata only; keep fonts + MotionProvider intact.
  app/globals.css - keeps @import tailwindcss + tokens + @theme + resets. The old .screen/.content/.wordmark/.tagline/.rule/.soon/@keyframes rise classes can be removed once page.tsx no longer uses them. KEEP the @theme block, resets, body styles, and the reduced-motion guard.
</interfaces>

LOCKED constraints:
- Phase 2 ships a SIMPLE static hero header only (no WebGL/3D/video here). Phases 3-4 replace/enhance it. The header must state the one-line value (agents interrogate + fight for credits/money) as readable text and surface the sponsor CTA in the first screen.
- Static export: keep CNAME/.nojekyll/basePath untouched (Phase-1 verify-build guard enforces this - `npm run verify:build` must still pass). No next/image optimization.
- Motion: SmoothScroll wraps the page; sections already animate via Reveal/ScrollParallax. Do NOT add new animation logic - only mount SmoothScroll. Reduced-motion remains base (gated inside SmoothScroll + primitives). Animations are transform/opacity/filter only (MOTION-03).

</context>

<tasks>

<task type="auto">
  <name>Task 1: Build static HeroHeader + Nav (first-screen value + sponsor CTA)</name>
  <files>components/sections/HeroHeader.tsx, components/ui/Nav.tsx</files>
  <read_first>
    - components/ui/SectionShell.tsx, GradientText.tsx, CTAButton.tsx (REUSE)
    - components/motion/Reveal.tsx (REUSE)
    - data/site.ts (brand, fullName, tagline, anchors.sponsor)
    - data/concept.ts (conceptCopy.headline / lede for the value line)
    - styles/tokens.css, app/globals.css (existing utility/token conventions)
  </read_first>
  <action>
Create `components/ui/Nav.tsx`. Top comment: `// Minimal top nav - brand + persistent sponsor CTA (keeps the ask above the fold).`
Import `{ site }` from `@/data/site`, `{ CTAButton }`. Export `function Nav()`. Render a `<nav>` (sticky top, subtle backdrop): brand wordmark (`site.brand`, display font) on the left, and a persistent `<CTAButton href={`#${site.anchors.sponsor}`}>Sponsor</CTAButton>` on the right. Keep it lightweight; real <a> anchors. Server Component.

Create `components/sections/HeroHeader.tsx`. Top comment: `// STATIC hero header (Phase 2). Phases 3-4 replace/enhance with poster + WebGL. States the value + surfaces sponsor CTA in the first screen.`
Import `{ site }` from `@/data/site`, `{ conceptCopy }` from `@/data/concept`, `{ GradientText }`, `{ CTAButton }`, `{ Reveal }`. Export `function HeroHeader()`.
Render a full-first-screen `<header>` (`minHeight: "100svh"`, flex column centered, generous padding) with a cinematic static background using ONLY CSS (deep gradient using --ufa-bg / --ufa-accent-iridescent at low opacity, e.g. a radial/linear gradient overlay - NO video, NO canvas):
- An eyebrow line (`site.fullName`, uppercase, letter-spaced, muted).
- A large `<h1>` (font-family var(--font-display), font-size var(--ufa-type-display), line-height ~1) stating the value - use `conceptCopy.headline` (states fight for money) and/or `site.tagline`; wrap the key phrase in `<GradientText>`. The H1 must remain legible with motion stripped and must convey "AI agents interrogate + fight for credits/money".
- A subhead `<p>` (`conceptCopy.lede` or a one-liner, --ufa-type-body, muted, max-width ~50ch).
- A primary `<CTAButton href={`#${site.anchors.sponsor}`}>Get in touch / Sponsor</CTAButton>` visible in the first screen, plus a secondary text link to `#${site.anchors.concept}` ("See how it works").
Wrap the H1/subhead/CTA in `<Reveal>` blocks (staggered) - reduced motion shows them immediately. Server Component. The hero must answer what/why/how-to-act in the first screen (anti style-over-substance).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "anchors.sponsor" components/sections/HeroHeader.tsx && grep -q "CTAButton" components/sections/HeroHeader.tsx && grep -q "GradientText" components/sections/HeroHeader.tsx && grep -q "100svh\|100vh\|minHeight" components/sections/HeroHeader.tsx && test -f components/ui/Nav.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/HeroHeader.tsx` exists, exports `HeroHeader`, renders an H1 value statement + a sponsor `CTAButton` linking to `#${site.anchors.sponsor}`, fills the first screen (min-height)
    - No video/canvas/WebGL in the hero (grep `video`/`canvas`/`three` returns nothing)
    - `components/ui/Nav.tsx` exists, exports `Nav`, has a persistent sponsor CTA
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A simple, strong static hero header states the UFA value and surfaces the sponsor CTA in the first screen using CSS-only visuals, plus a nav with a persistent sponsor CTA - both legible with motion stripped, ready for Phase 3/4 to upgrade.</done>
</task>

<task type="auto">
  <name>Task 2: Compose app/page.tsx in conversion order + mount SmoothScroll</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current Coming Soon placeholder - REPLACE content)
    - components/motion/SmoothScroll.tsx (REUSE - wrap page once)
    - components/sections/HeroHeader.tsx + all Wave-2 sections + components/ui/Nav.tsx
    - .planning/research/ARCHITECTURE.md (Pattern 1: thin composition root)
  </read_first>
  <action>
Replace the contents of `app/page.tsx`. Top comment: `// Composition root - the "edit": orders sections in the LOCKED conversion-optimized scroll order.`

Import the sections and `{ SmoothScroll }` from `@/components/motion/SmoothScroll`, `{ Nav }` from `@/components/ui/Nav`. Export `default function Home()`.

Render (in EXACTLY this locked order):
```tsx
<SmoothScroll>
  <Nav />
  <main>
    <HeroHeader />
    <Concept />
    <HowItWorks />
    <MatchShowcase />
    <PrizePool />
    <Standings />
    <SponsorValue />
    <Traction />
    <Lineage />
    <SponsorCTA />
    <Footer />
  </main>
</SmoothScroll>
```
(Order follows the locked narrative: static hero -> Concept -> How It Works -> Spectacle/Match Showcase -> Prize Pool -> Standings -> Sponsor-value + logo wall -> Traction strip -> Lineage -> repeated CTA + social proof -> Footer. If the locked order in CONTEXT/decisions places Lineage earlier, follow the decision; otherwise use this order which keeps Lineage as a positioning beat before the final CTA.)

`SmoothScroll` is a "use client" behavior wrapper with no DOM box - mounting it at the root is correct and it early-returns to native scroll under reduced motion. `app/page.tsx` stays otherwise a Server Component (it just imports a client child). Remove all Coming Soon markup. No animation logic added here - sections own their own Reveal/ScrollParallax.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "SmoothScroll" app/page.tsx && grep -q "HeroHeader" app/page.tsx && grep -q "SponsorCTA" app/page.tsx && grep -q "Footer" app/page.tsx && ! grep -q "Coming soon" app/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` renders `<SmoothScroll>` wrapping `<Nav/>` + `<main>` with all 11 beats (HeroHeader, Concept, HowItWorks, MatchShowcase, PrizePool, Standings, SponsorValue, Traction, Lineage, SponsorCTA, Footer)
    - The Coming Soon placeholder markup is gone (grep "Coming soon" returns nothing)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>app/page.tsx is the thin composition root ordering every section in the locked conversion sequence, wrapped once in SmoothScroll, with the placeholder removed.</done>
</task>

<task type="auto">
  <name>Task 3: Update layout metadata, clean globals, verify build + reduced-motion</name>
  <files>app/layout.tsx, app/globals.css</files>
  <read_first>
    - app/layout.tsx (metadata to update; keep fonts + MotionProvider)
    - app/globals.css (keep @theme/resets/body/reduced-motion guard; remove dead Coming Soon classes)
    - data/site.ts (fullName, tagline for metadata copy)
    - .planning/research/PITFALLS.md ("Looks Done But Isn't" - reduced-motion + build artifact checks)
    - scripts/verify-build.mjs (what the build guard asserts)
  </read_first>
  <action>
Update `app/layout.tsx` metadata ONLY (do NOT touch the font setup or MotionProvider wrapper). Replace the "coming soon" strings with real-site copy:
- `title`: "Ultimate Agent Fight - AI agents fight for real money, live" (or similar from data/site - it's fine to inline the string; keep it under ~60 chars where possible).
- `description`: one sentence stating AI agents interrogate + fight for credits/money and inviting sponsors to get in touch.
- `openGraph.title`/`description`/`siteName`: matching real copy; keep `url`/`metadataBase` as https://ufa.foundation and `type: "website"`.
Keep the existing static opengraph-image (Phase 1) working - do not remove it.

Edit `app/globals.css`: REMOVE the now-dead Coming Soon utility classes (`.screen`, `.content`, `.wordmark`, `.tagline`, `.rule`, `.soon`, `@keyframes rise`, and their reduced-motion override) since page.tsx no longer uses them. KEEP: `@import "tailwindcss"`, `@import "../styles/tokens.css"`, the entire `@theme` block, the `*`/`html,body` resets, and the `body` base styles. Ensure a global reduced-motion safety net remains or is added: a `@media (prefers-reduced-motion: reduce)` rule that sets `*, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; scroll-behavior: auto !important; }` as a CSS-level backstop (the JS primitives already gate, this is defense-in-depth).

Then run the build + verify gates: `npm run typecheck`, `npm run build`, `npm run verify:build`, and the reduced-motion smoke gate `node --test components/motion/reduced-motion.smoke.mjs`. All must pass; CNAME + .nojekyll must remain in out/.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && npm run verify:build && node --test components/motion/reduced-motion.smoke.mjs && test -f out/CNAME && test -f out/.nojekyll && grep -q "Ultimate Agent Fight" app/layout.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` metadata describes the real site (title/description/OG mention Ultimate Agent Fight + the concept); fonts + MotionProvider untouched
    - `app/globals.css` no longer contains `.wordmark`/`.soon`/`@keyframes rise`; still contains `@theme` + body resets + a `prefers-reduced-motion` rule
    - `npm run build` exits 0 and `out/CNAME` + `out/.nojekyll` exist (verify:build passes)
    - reduced-motion smoke gate passes
  </acceptance_criteria>
  <done>Metadata describes the real site, dead placeholder CSS is removed (a reduced-motion CSS backstop remains), and the full static export builds green with CNAME/.nojekyll intact and the reduced-motion contract verified.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; `npm run verify:build` passes (out/CNAME + out/.nojekyll + hashed Tailwind CSS present)
- `node --test components/motion/reduced-motion.smoke.mjs` passes (reduced-motion remains base case)
- `app/page.tsx` renders the full ordered scroll wrapped in SmoothScroll; "Coming soon" markup gone
- HeroHeader states the value + sponsor CTA in the first screen with CSS-only visuals (no WebGL/video)
- layout metadata updated to the real site; opengraph-image still emitted
- No `motion/react` reimplementation in page/sections beyond the Phase-1 primitives
</verification>

<success_criteria>
The deployed-shaped static export is a complete, impressive, sendable landing page: a strong static hero header states what UFA is and surfaces the sponsor CTA in the first screen; every section follows in the locked conversion order with scroll-driven motion via the Phase-1 primitives; SmoothScroll eases the spine with reduced-motion as the base case; and the build is green with CNAME/.nojekyll intact and real share metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-conversion-spine/02-05-SUMMARY.md`
</output>
