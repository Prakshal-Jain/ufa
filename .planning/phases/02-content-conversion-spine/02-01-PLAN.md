---
phase: 02-content-conversion-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/site.ts
  - data/concept.ts
  - data/matches.ts
  - data/prizes.ts
  - data/standings.ts
  - data/sponsors.ts
  - data/traction.ts
  - data/endorsements.ts
  - ASSETS-NEEDED.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every content-dependent section can import a typed placeholder dataset from data/*.ts so swapping real values is a one-file edit"
    - "A repo-root ASSETS-NEEDED.md checklist tells the user exactly what real assets to provide and which data file each maps to"
  artifacts:
    - path: "data/site.ts"
      provides: "Site-wide strings: brand, tagline, nav anchors, contact email, social links, CTA targets, Formspree fallback id"
      contains: "export"
    - path: "data/concept.ts"
      provides: "Concept copy + how-it-works steps + UFC->UFB->UFA lineage data"
      contains: "credits"
    - path: "data/matches.ts"
      provides: "Match showcase clip/poster entries"
      contains: "export"
    - path: "data/prizes.ts"
      provides: "Prize pool / credits tiers"
      contains: "export"
    - path: "data/standings.ts"
      provides: "Standings/leaderboard teaser rows"
      contains: "export"
    - path: "data/sponsors.ts"
      provides: "Sponsor-value props + partner logo wall entries"
      contains: "export"
    - path: "data/traction.ts"
      provides: "Traction/metrics strip figures"
      contains: "export"
    - path: "data/endorsements.ts"
      provides: "Endorsement/social-proof quotes adjacent to CTA"
      contains: "export"
    - path: "ASSETS-NEEDED.md"
      provides: "User checklist of real assets to supply"
      contains: "data/"
  key_links:
    - from: "components/sections/*"
      to: "data/*.ts"
      via: "typed build-time import"
      pattern: "from \"@/data/"
---

<objective>
Create the typed, build-time local data layer that drives every content-dependent section of the page, plus a repo-root ASSETS-NEEDED.md checklist telling the user exactly what real assets to provide and where each lives. All values are polished, representative PLACEHOLDER content (clearly marked in code comments) so the page reads compelling and real today, and swapping in real content later is a single-file edit.

Purpose: Decouple content from presentation (research ARCHITECTURE Pattern 5). This unblocks all Wave-2 section plans, which import these typed exports rather than hardcoding copy.
Output: data/site.ts, data/concept.ts, data/matches.ts, data/prizes.ts, data/standings.ts, data/sponsors.ts, data/traction.ts, data/endorsements.ts, and ASSETS-NEEDED.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- The data layer is plain typed TS exports; it needs no code interfaces. Downstream Wave-2 sections consume these via the "@/*" path alias (tsconfig.json -> repo root), e.g. import { matches } from "@/data/matches". -->
From styles/tokens.ts: tokens.{bg,fg,accent,accent2,accent3} colors exist for downstream sections.
</interfaces>

Static-export constraint: data is resolved at build time (plain typed TS exports — no fetch/API/CMS). Any asset paths referenced in data must be root-relative strings under public/ (e.g. "/media/match-01-poster.jpg") so they resolve from the apex root with images.unoptimized + trailingSlash:true.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create site + concept/how-it-works/lineage data modules</name>
  <files>data/site.ts, data/concept.ts</files>
  <read_first>
    - .planning/PROJECT.md (core value, positioning, contact intent)
    - .planning/research/FEATURES.md (Concept block, How-it-works, UFC->UFB->UFA lineage definitions)
    - styles/tokens.ts (available accent colors for downstream sections)
  </read_first>
  <action>
Create `data/site.ts`. Top comment line: `// PLACEHOLDER content - swap real values here; see ASSETS-NEEDED.md.` Export typed constants:

```ts
export interface SocialLink { label: string; href: string; }
export const site = {
  brand: "UFA",
  fullName: "Ultimate Agent Fight",
  tagline: "AI agents interrogate and fight for real credits and real money.",
  contactEmail: "sponsor@ufa.foundation",
  // CTA targets - in-page anchors used by nav + repeated CTA buttons
  anchors: {
    concept: "concept",
    howItWorks: "how-it-works",
    matches: "matches",
    prizes: "prizes",
    standings: "standings",
    sponsor: "sponsor",
  },
  socials: [
    { label: "X", href: "https://x.com/" },
    { label: "YouTube", href: "https://youtube.com/" },
    { label: "Twitch", href: "https://twitch.tv/" },
  ] as SocialLink[],
  // Formspree: real endpoint comes from NEXT_PUBLIC_FORMSPREE_ENDPOINT (env).
  // This is the clearly-marked placeholder form id the user swaps if env is unset.
  formspreeFallbackId: "xpwzgkqr", // PLACEHOLDER - replace with real Formspree form id
} as const;
```

Create `data/concept.ts`. Same PLACEHOLDER comment line. Export typed interfaces + values:
- `conceptCopy`: object with `headline` (e.g. "AI agents fight for real money - live."), `lede` (2-3 plain-language sentences stating agents INTERROGATE then FIGHT each other to win CREDITS and MONEY - the word "credits" MUST appear), and `points: string[]` (3 short clarity bullets).
- `howItWorks: { step: number; title: string; body: string }[]` - exactly 4 steps describing the match format: (1) agents are matched, (2) interrogation round (agents probe each other), (3) the fight (head-to-head challenge), (4) winner takes credits + cash. Real, legible copy - NOT lorem ipsum.
- `lineage: { node: string; blurb: string }[]` - exactly 3 entries: UFC (the original - humans), UFB (the bridge - bots/robots), UFA (the next step - AI agents). Plus `lineageStatement: string` framing UFA as "the UFC of AI."

Define and export TypeScript interfaces for each shape so downstream sections get types. Copy must read fully and convert with zero motion (substance-over-style mandate).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "credits" data/concept.ts && grep -q "formspreeFallbackId" data/site.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `data/site.ts` exists and exports `site` with `anchors.sponsor` and `formspreeFallbackId`
    - `data/concept.ts` exports `conceptCopy`, `howItWorks` (length 4), and `lineage` (length 3)
    - `grep -q "credits" data/concept.ts` succeeds (concept states credits/money)
    - `grep -ri "lorem ipsum" data/concept.ts data/site.ts` returns nothing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>site.ts and concept.ts compile, contain real placeholder copy stating the UFA concept (interrogate + fight for credits/money) and the UFC->UFB->UFA lineage, with typed exports for downstream sections.</done>
</task>

<task type="auto">
  <name>Task 2: Create showcase/prize/standings/sponsor/traction/endorsement data modules</name>
  <files>data/matches.ts, data/prizes.ts, data/standings.ts, data/sponsors.ts, data/traction.ts, data/endorsements.ts</files>
  <read_first>
    - .planning/research/FEATURES.md (Match showcase, Prize pool, Standings teaser, Sponsor-value framing, Traction metrics strip, Endorsements - content dependencies + "founding sponsors wanted" honest framing)
    - data/site.ts (created Task 1 - reuse anchor/contact conventions if referenced)
  </read_first>
  <action>
Each file starts with `// PLACEHOLDER content - swap real values here; see ASSETS-NEEDED.md.` Export typed interfaces + arrays. Use representative, compelling, honest "Season 1 - founding sponsors wanted" framing so nothing reads empty. Asset paths are root-relative strings under /media (files do NOT exist yet - expected; sections render poster fallbacks / honest placeholders).

`data/matches.ts`:
```ts
export interface Match { id: string; title: string; agents: [string, string]; poster: string; clip?: string; result: string; }
export const matches: Match[] = [ /* 3 entries, e.g. */
  { id: "m1", title: "Season 1 - Opening Bout", agents: ["Agent Vega", "Agent Mercer"], poster: "/media/match-01-poster.jpg", result: "Vega by decision" },
  // ...2 more
];
```
poster paths point under /media (no autoplay sound; clip optional and lazy - sections handle <video> attrs).

`data/prizes.ts`: `interface PrizeTier { label: string; amount: string; note?: string }` and `prizePool: PrizeTier[]` (e.g. Grand Prize "$50,000" + "compute credits", Runner-up "$15,000"), plus a `totalCredits` headline string like "1,000,000 compute credits at stake". Concrete numbers.

`data/standings.ts`: `interface StandingRow { rank: number; agent: string; wins: number; losses: number; rating: number }` and `standings: StandingRow[]` (5 rows, descending rank). Add `standingsNote: string` ("Season 1 snapshot - updated manually").

`data/sponsors.ts`: export `sponsorValue: { headline: string; pitch: string; audience: string[] }` (audience like "SF AI builders", "frontier-lab researchers", "investors at the AI frontier"; state reach value, e.g. pitch includes "Reach 10k+ AI builders at the frontier"). Also `interface Partner { name: string; logo: string }` and `partners: Partner[]` (4-6 entries with /media/logos/*.svg paths) plus `partnersNote: string` ("Founding partners - your logo here") so an early-stage wall never reads dead.

`data/traction.ts`: `interface Metric { value: string; label: string }` and `metrics: Metric[]` (4 entries: matches run, total prize awarded, agents competed, viewers - concrete-but-modest numbers).

`data/endorsements.ts`: `interface Endorsement { quote: string; name: string; role: string }` and `endorsements: Endorsement[]` (2-3 short credible quotes placed adjacent to the CTA for social-proof lift).

All copy real and legible; no lorem ipsum.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "prizePool" data/prizes.ts && grep -q "standings" data/standings.ts && grep -q "partners" data/sponsors.ts && grep -q "metrics" data/traction.ts && grep -q "endorsements" data/endorsements.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist and export their named arrays/objects (`matches`, `prizePool`, `standings`, `sponsorValue`+`partners`, `metrics`, `endorsements`)
    - `data/prizes.ts` contains a concrete dollar figure (grep "\$" succeeds)
    - `data/sponsors.ts` contains a reach/audience claim (grep -i "reach\|builders" succeeds)
    - `grep -ri "lorem ipsum" data/` returns nothing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All six content modules compile with typed exports, concrete placeholder figures, honest early-stage framing, and /media-relative asset path strings ready for Wave-2 sections to consume.</done>
</task>

<task type="auto">
  <name>Task 3: Create ASSETS-NEEDED.md checklist</name>
  <files>ASSETS-NEEDED.md</files>
  <read_first>
    - data/site.ts, data/concept.ts, data/matches.ts, data/prizes.ts, data/standings.ts, data/sponsors.ts, data/traction.ts, data/endorsements.ts (Tasks 1-2 - enumerate exactly what each expects)
  </read_first>
  <action>
Create `ASSETS-NEEDED.md` at repo root. A markdown checklist the user fills in to go from placeholder -> real. Structure:

- Title + one-line intro: "Everything below is currently polished PLACEHOLDER content. Swap each item in the listed data file (one-file edits) to ship the real site."
- A checklist (use `- [ ]` items) covering EVERY content-dependent area, naming the data file + field for each row:
  - Match clips + posters -> `data/matches.ts` (`poster`, `clip`); note: 720p H.264 MP4, muted/loop/playsinline, ~3-4MB; poster JPG; files go in `public/media/`.
  - Prize figures -> `data/prizes.ts` (`prizePool`, `totalCredits`).
  - Standings rows -> `data/standings.ts` (`standings`).
  - Sponsor logos -> `data/sponsors.ts` (`partners[].logo`); note: SVG/PNG in `public/media/logos/`.
  - Traction metrics -> `data/traction.ts` (`metrics`).
  - Endorsement quotes -> `data/endorsements.ts` (`endorsements`).
  - Contact email + socials + Formspree id -> `data/site.ts` (`contactEmail`, `socials`, `formspreeFallbackId`) and the `NEXT_PUBLIC_FORMSPREE_ENDPOINT` env var.
- A short "How to swap" note: edit the data file, drop assets in public/media, rebuild.
  </action>
  <verify>
    <automated>test -f ASSETS-NEEDED.md && grep -q "data/matches.ts" ASSETS-NEEDED.md && grep -q "data/prizes.ts" ASSETS-NEEDED.md && grep -q "data/sponsors.ts" ASSETS-NEEDED.md && grep -q "data/standings.ts" ASSETS-NEEDED.md && grep -q "data/traction.ts" ASSETS-NEEDED.md && grep -q "NEXT_PUBLIC_FORMSPREE_ENDPOINT" ASSETS-NEEDED.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ASSETS-NEEDED.md` exists at repo root
    - It lists matches, prizes, logos, standings, traction, and endorsements, each mapped to its `data/*.ts` file
    - It references `NEXT_PUBLIC_FORMSPREE_ENDPOINT`
    - Uses `- [ ]` checkbox items
  </acceptance_criteria>
  <done>ASSETS-NEEDED.md exists at repo root, enumerates every placeholder asset the user must supply, and maps each to its data file + public/media location.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (all data modules type-check)
- `grep -ri "lorem ipsum" data/` returns nothing
- `data/concept.ts` contains "credits"; `data/prizes.ts` contains a "$" figure
- `ASSETS-NEEDED.md` present at repo root and references each data file + the Formspree env var
- No section/page files touched (this plan owns only data/ + ASSETS-NEEDED.md)
</verification>

<success_criteria>
A complete, typed, build-time data layer exists that every Wave-2 section can import, all values are clearly-marked compelling placeholders with honest early-stage framing, and the user has a single checklist (ASSETS-NEEDED.md) for turning placeholders into real content via one-file edits.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-conversion-spine/02-01-SUMMARY.md`
</output>
