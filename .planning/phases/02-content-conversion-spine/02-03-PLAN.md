---
phase: 02-content-conversion-spine
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - components/sections/MatchShowcase.tsx
  - components/sections/PrizePool.tsx
  - components/sections/Standings.tsx
autonomous: true
requirements: [SHOW-01, SHOW-02, PRIZE-01, PRIZE-02]
must_haves:
  truths:
    - "Visitor can view a match/spectacle showcase built to accept swappable placeholder media, performant on mobile (no autoplay sound, no jank)"
    - "Visitor sees a concrete prize pool / credits at stake"
    - "Visitor sees a standings/leaderboard teaser of competing agents"
    - "All three sections read fully with motion stripped"
  artifacts:
    - path: "components/sections/MatchShowcase.tsx"
      provides: "Match showcase rendering matches[] with poster + optional muted/loop/playsinline video"
      contains: "data/matches"
      min_lines: 30
    - path: "components/sections/PrizePool.tsx"
      provides: "Prize pool / credits display"
      contains: "prizePool"
      min_lines: 25
    - path: "components/sections/Standings.tsx"
      provides: "Standings teaser table"
      contains: "standings"
      min_lines: 25
  key_links:
    - from: "components/sections/MatchShowcase.tsx"
      to: "data/matches.ts"
      via: "typed import"
      pattern: "from \"@/data/matches\""
    - from: "components/sections/MatchShowcase.tsx"
      to: "muted/loop/playsinline video attributes"
      via: "mobile-safe media"
      pattern: "playsInline"
---

<objective>
Build the three proof-and-stakes sections: MatchShowcase (proof it's alive), PrizePool (the stakes), and Standings (a living-league teaser). Each is a self-contained component under components/sections/, reuses Phase-1 primitives, and renders typed placeholder data from Plan 01. Media is mobile-safe by construction (poster-first, muted/loop/playsinline, no autoplay sound).

Purpose: Supply the credibility engine (real-match proof + concrete prize numbers + standings) that converts sponsors, built to swap real assets via one-file data edits.
Output: components/sections/MatchShowcase.tsx, PrizePool.tsx, Standings.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Phase-1 primitives to REUSE - do NOT reimplement animation. -->
From components/ui/SectionShell.tsx: function SectionShell({ children, id?, className? }).
From components/ui/GradientText.tsx: function GradientText({ children, className? }).
From components/motion/Reveal.tsx ("use client"): function Reveal({ children, y=24, delay=0 }) - reduced-motion renders children directly.
From components/motion/ScrollParallax.tsx ("use client"): function ScrollParallax({ children, range=80 }).

Data (Plan 01):
  data/matches.ts -> matches: { id, title, agents:[string,string], poster:string, clip?:string, result:string }[]
  data/prizes.ts  -> prizePool: { label, amount, note? }[]; totalCredits: string
  data/standings.ts -> standings: { rank, agent, wins, losses, rating }[]; standingsNote: string
  data/site.ts -> site.anchors.{matches, prizes, standings}
  (Confirm exact field names against Plan 01 output.)

Tokens: --ufa-type-h2, --ufa-type-body, --ufa-type-display, --ufa-muted, --ufa-accent, --ufa-accent-iridescent, --ufa-bg-elevated, --ufa-rule, --ufa-radius, --ufa-glow. Path alias "@/*".
</interfaces>

Static-export + mobile constraints (research PITFALLS 3 + anti-pattern 1):
- images.unoptimized is set; do NOT use next/image optimization. Use plain `<img>` with explicit width/height or aspect-ratio to avoid CLS, OR <img loading="lazy">. Poster paths are root-relative (/media/...).
- Any `<video>` MUST be `muted loop playsInline` and use `poster={match.poster}` with `preload="none"`; NO autoplay sound. Reserve aspect-ratio so there's no layout shift. Below-the-fold clips lazy by default (don't eagerly load).
- The /media files do not exist yet - that's expected. The poster `<img>` will 404 gracefully; do NOT block on missing assets. Add `alt` text from match data.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MatchShowcase section (SHOW-01, SHOW-02)</name>
  <files>components/sections/MatchShowcase.tsx</files>
  <read_first>
    - components/ui/SectionShell.tsx (REUSE)
    - components/motion/Reveal.tsx (REUSE)
    - data/matches.ts (Match shape)
    - .planning/research/PITFALLS.md (mobile video: muted/loop/playsinline/poster; no autoplay sound; lazy below-fold)
    - styles/tokens.css
  </read_first>
  <action>
Create `components/sections/MatchShowcase.tsx`. Top comment: `// Match showcase - swappable placeholder media (data/matches.ts). Mobile-safe: poster-first, muted/loop/playsinline, no autoplay sound.`

Import `{ matches }` from `@/data/matches`, `{ site }` from `@/data/site`, `{ SectionShell }`, `{ GradientText }`, `{ Reveal }`. Export `function MatchShowcase()`.

Render `<SectionShell id={site.anchors.matches}>`:
- `<h2>` e.g. "Real matches. Real stakes." (display font, --ufa-type-h2; emphasize a phrase with GradientText).
- A responsive grid (`gridTemplateColumns: "repeat(auto-fit, minmax(280px, 1fr))"`, gap 1.25rem) mapping `matches` to cards, each wrapped in `<Reveal delay={i*0.08}>`. Each card:
  - A media frame with FIXED aspect ratio via inline `style={{ aspectRatio: "16/9" }}` (prevents CLS) and `overflow: hidden`, `borderRadius: var(--ufa-radius)`, `background: var(--ufa-bg-elevated)`.
  - Inside: a plain `<img src={match.poster} alt={`${match.agents[0]} vs ${match.agents[1]}`} loading="lazy" style={{ width:"100%", height:"100%", objectFit:"cover" }} />`. (If `match.clip` is present you MAY render a `<video muted loop playsInline preload="none" poster={match.poster}>` with a `<source src={match.clip} />` instead - but keep autoplay OFF / no sound; a poster-only card is acceptable and is the safe default.)
  - Card caption: `match.title` (small accent label), `match.agents` as "A vs B" (display font), and `match.result` (muted).

Mobile-first: single column stacks naturally via auto-fit. No autoplay-with-sound anywhere. Server Component.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "from \"@/data/matches\"" components/sections/MatchShowcase.tsx && grep -q "playsInline\|loading=\"lazy\"" components/sections/MatchShowcase.tsx && grep -q "aspectRatio\|aspect-ratio" components/sections/MatchShowcase.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/MatchShowcase.tsx` exists, exports `MatchShowcase`, maps `matches`
    - Media frame reserves space (grep `aspectRatio` or `aspect-ratio`) to avoid CLS
    - Uses lazy `<img loading="lazy">` and/or `<video ... playsInline>` with no sound autoplay
    - Section root carries `id={site.anchors.matches}`; uses `SectionShell` + `Reveal`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MatchShowcase renders a responsive, CLS-safe, mobile-safe (no autoplay sound) grid of match cards from swappable typed data, reusing Phase-1 primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Build PrizePool section (PRIZE-01)</name>
  <files>components/sections/PrizePool.tsx</files>
  <read_first>
    - components/ui/SectionShell.tsx, components/ui/GradientText.tsx (REUSE)
    - components/motion/Reveal.tsx (REUSE)
    - data/prizes.ts (prizePool tiers + totalCredits)
    - styles/tokens.css
  </read_first>
  <action>
Create `components/sections/PrizePool.tsx`. Top comment: `// Prize pool / credits at stake. Figures from data/prizes.ts (placeholder).`

Import `{ prizePool, totalCredits }` from `@/data/prizes`, `{ site }` from `@/data/site`, `{ SectionShell }`, `{ GradientText }`, `{ Reveal }`. Export `function PrizePool()`.

Render `<SectionShell id={site.anchors.prizes}>`:
- A large hero number: `totalCredits` rendered very large (font-size: var(--ufa-type-display) or close), wrapped in `<GradientText>` with a `<Reveal>`. This is the "stakes" punch.
- A `<h2>`/eyebrow context line ("Prize pool", muted).
- A row/grid of `prizePool` tiers (`repeat(auto-fit, minmax(180px,1fr))`), each in a card: `tier.amount` large (accent or fg), `tier.label` (display font), `tier.note` (muted) when present. Wrap each in `<Reveal delay={i*0.08}>`.

Concrete numbers must be visible. Server Component. Apply `var(--ufa-glow)` as a subtle box-shadow on the hero number container for the cinematic feel (transform/opacity-safe - box-shadow is static, not animated).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "from \"@/data/prizes\"" components/sections/PrizePool.tsx && grep -q "prizePool" components/sections/PrizePool.tsx && grep -q "SectionShell" components/sections/PrizePool.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/PrizePool.tsx` exists, exports `PrizePool`, renders `totalCredits` and maps `prizePool`
    - Uses `SectionShell`, `GradientText`, `Reveal`
    - Section root carries `id={site.anchors.prizes}`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PrizePool prominently displays concrete prize money + credits at stake from typed data, reusing Phase-1 primitives, legible with motion off.</done>
</task>

<task type="auto">
  <name>Task 3: Build Standings teaser section (PRIZE-02)</name>
  <files>components/sections/Standings.tsx</files>
  <read_first>
    - components/ui/SectionShell.tsx (REUSE)
    - components/motion/Reveal.tsx (REUSE)
    - data/standings.ts (standings rows + standingsNote)
    - styles/tokens.css
  </read_first>
  <action>
Create `components/sections/Standings.tsx`. Top comment: `// Standings/leaderboard teaser (static snapshot). Rows from data/standings.ts (placeholder).`

Import `{ standings, standingsNote }` from `@/data/standings`, `{ site }` from `@/data/site`, `{ SectionShell }`, `{ Reveal }`. Export `function Standings()`.

Render `<SectionShell id={site.anchors.standings}>`:
- `<h2>` "Standings" (display font).
- A real semantic `<table>` (header row: Rank, Agent, W, L, Rating) mapping `standings` rows. Style with inline styles: full width, `borderCollapse: "collapse"`, row separators via `borderBottom: 1px solid var(--ufa-rule)`, rank/rating in accent color, comfortable cell padding, monospace-ish numerics optional. Top rank gets subtle emphasis (e.g. accent text). Keep it readable on mobile (allow horizontal scroll wrapper: a `<div style={{ overflowX: "auto" }}>` around the table).
- `standingsNote` rendered beneath in muted small text ("Season 1 snapshot - updated manually") to honestly frame the static teaser.
- Wrap the table block in a single `<Reveal>`.

Static teaser only - NO sorting/live data (matches the no-backend constraint). Server Component.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "from \"@/data/standings\"" components/sections/Standings.tsx && grep -q "standings" components/sections/Standings.tsx && grep -q "<table" components/sections/Standings.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/Standings.tsx` exists, exports `Standings`, maps `standings` into a `<table>`
    - Renders `standingsNote`; section root carries `id={site.anchors.standings}`
    - Uses `SectionShell` + `Reveal`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Standings renders a static, mobile-scrollable leaderboard teaser of competing agents with an honest "snapshot" note, reusing Phase-1 primitives.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- MatchShowcase uses poster-first, lazy, no-autoplay-sound media with reserved aspect-ratio (grep `aspectRatio`/`playsInline`/`loading="lazy"`)
- All three sections import from their `@/data/*` module and reuse `SectionShell` + Reveal
- No `motion/react` import in components/sections/ (animation comes only from Phase-1 primitives)
- PrizePool shows a concrete figure; Standings renders a real `<table>` + honest note
</verification>

<success_criteria>
A visitor sees proof the competition is alive (swappable match media, mobile-safe), the concrete prize/credits stakes, and a standings teaser implying a living league - all from typed placeholder data, all reusing Phase-1 primitives, all legible with motion off. Ready for Plan 05 assembly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-conversion-spine/02-03-SUMMARY.md`
</output>
