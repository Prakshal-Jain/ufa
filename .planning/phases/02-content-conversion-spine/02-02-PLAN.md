---
phase: 02-content-conversion-spine
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - components/sections/Concept.tsx
  - components/sections/HowItWorks.tsx
  - components/sections/Lineage.tsx
autonomous: true
requirements: [CONCEPT-01, CONCEPT-02, CONCEPT-03]
must_haves:
  truths:
    - "Visitor reads a clear plain-language explanation that UFA is AI agents interrogating + fighting for credits and money"
    - "Visitor understands how a fight works via a 4-step how-it-works section"
    - "Visitor sees the UFC -> UFB -> UFA lineage framing"
    - "All three sections read fully and legibly with motion stripped (reduced-motion base case)"
  artifacts:
    - path: "components/sections/Concept.tsx"
      provides: "Concept explainer rendering conceptCopy"
      contains: "data/concept"
      min_lines: 25
    - path: "components/sections/HowItWorks.tsx"
      provides: "4-step match-format breakdown"
      contains: "howItWorks"
      min_lines: 25
    - path: "components/sections/Lineage.tsx"
      provides: "UFC -> UFB -> UFA brand beat"
      contains: "lineage"
      min_lines: 25
  key_links:
    - from: "components/sections/Concept.tsx"
      to: "data/concept.ts"
      via: "typed import"
      pattern: "from \"@/data/concept\""
    - from: "components/sections/Concept.tsx"
      to: "components/ui/SectionShell + components/motion/Reveal"
      via: "consume Phase-1 primitives"
      pattern: "SectionShell|Reveal"
---

<objective>
Build the three narrative-foundation sections that make the novel concept legible: Concept (what UFA is), HowItWorks (how a fight works), and Lineage (UFC -> UFB -> UFA). Each is a self-contained component under components/sections/, consumes the Phase-1 UI + motion primitives (never reimplements animation), and renders typed copy from data/concept.ts.

Purpose: Satisfy the substance-over-style mandate - the visitor must understand what UFA is in one read, with the message fully intact when motion is stripped.
Output: components/sections/Concept.tsx, HowItWorks.tsx, Lineage.tsx (assembled into the page in Plan 05).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/FEATURES.md

<interfaces>
<!-- Phase-1 primitives to REUSE. Do NOT reimplement animation; reduced-motion is already the base case inside Reveal/ScrollParallax. -->
From components/ui/SectionShell.tsx (Server Component):
  function SectionShell({ children, id?, className? }): applies vertical rhythm + centered max-width container.
From components/ui/GradientText.tsx (Server Component):
  function GradientText({ children, className? }): iridescent clipped text with AA solid-color fallback.
From components/motion/Reveal.tsx ("use client"):
  function Reveal({ children, y=24, delay=0 }): whileInView fade/slide; reduced-motion -> renders children directly, fully visible. Transform/opacity only.
From components/motion/ScrollParallax.tsx ("use client"):
  function ScrollParallax({ children, range=80 }): transform-only translateY parallax; reduced-motion -> plain <div>.

Data (from Plan 01, data/concept.ts):
  conceptCopy: { headline: string; lede: string; points: string[] }
  howItWorks: { step: number; title: string; body: string }[]   // length 4
  lineage: { node: string; blurb: string }[]                     // length 3
  lineageStatement: string
  (Exact field names: confirm against the data file written in Plan 01.)
From data/site.ts: site.anchors.{concept, howItWorks} for section id values.

Tokens (styles/tokens.css --ufa-* vars): --ufa-type-display, --ufa-type-h2, --ufa-type-body, --ufa-muted, --ufa-accent, --ufa-accent-iridescent, --ufa-rule, --ufa-bg-elevated, --ufa-radius. Path alias "@/*" -> repo root.
</interfaces>

Static-export constraint: Server Components by default; mark a section "use client" ONLY if it directly imports Reveal/ScrollParallax (those are client). A Server section may still render <Reveal> children because Reveal is a client component imported into a server tree - that is allowed and preferred (keeps the section a server component). Use the existing `Reveal` import directly. Animate transform/opacity only (handled inside the primitives).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Concept section (CONCEPT-01)</name>
  <files>components/sections/Concept.tsx</files>
  <read_first>
    - components/ui/SectionShell.tsx, components/ui/GradientText.tsx (REUSE)
    - components/motion/Reveal.tsx (REUSE - wrap content)
    - data/concept.ts (conceptCopy shape)
    - styles/tokens.css (--ufa-* vars)
  </read_first>
  <action>
Create `components/sections/Concept.tsx`. Top comment: `// Concept section - "what UFA is" explainer. Copy from data/concept.ts (placeholder).`

Import `{ conceptCopy }` from `@/data/concept`, `{ site }` from `@/data/site`, `{ SectionShell }`, `{ GradientText }`, `{ Reveal }`. Export `function Concept()`.

Render a `<SectionShell id={site.anchors.concept}>` containing:
- An eyebrow/kicker line ("What is UFA?") styled small/uppercase using `color: var(--ufa-muted)`.
- An `<h2>` using `font-family: var(--font-display)`, `font-size: var(--ufa-type-h2)`, wrapping the key phrase in `<GradientText>` (e.g. render `conceptCopy.headline`). The headline states the concept; the word "credits"/"money" is present via the data.
- A `<p>` for `conceptCopy.lede` at `font-size: var(--ufa-type-body)`, `color: var(--ufa-fg)`, max-width ~60ch for readability.
- A `<ul>` mapping `conceptCopy.points` to `<li>` clarity bullets (muted color, comfortable spacing).

Wrap the heading block in one `<Reveal>` and the lede+points in a second `<Reveal delay={0.1}>` for a staggered entrance. Because reduced-motion is the base case inside Reveal, the section reads fully without motion. Use inline `style` objects with --ufa-* vars (matches existing primitive convention). Keep it a Server Component (no "use client" - Reveal is the only client piece and it is imported as a client child).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "from \"@/data/concept\"" components/sections/Concept.tsx && grep -q "SectionShell" components/sections/Concept.tsx && grep -q "Reveal" components/sections/Concept.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/Concept.tsx` exists, exports `Concept`, imports from `@/data/concept`
    - Uses `SectionShell`, `GradientText`, and `Reveal` (grep each succeeds)
    - Renders `conceptCopy.headline`, `conceptCopy.lede`, and maps `conceptCopy.points`
    - Section root carries `id={site.anchors.concept}`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Concept section renders the plain-language UFA explainer (interrogate + fight for credits/money) from typed data, reuses Phase-1 primitives, and reads fully with motion stripped.</done>
</task>

<task type="auto">
  <name>Task 2: Build HowItWorks section (CONCEPT-02)</name>
  <files>components/sections/HowItWorks.tsx</files>
  <read_first>
    - components/ui/SectionShell.tsx (REUSE)
    - components/motion/Reveal.tsx (REUSE - stagger steps)
    - data/concept.ts (howItWorks: 4 steps)
    - styles/tokens.css
  </read_first>
  <action>
Create `components/sections/HowItWorks.tsx`. Top comment: `// How-it-works section - 4-step match format. Steps from data/concept.ts (placeholder).`

Import `{ howItWorks }` from `@/data/concept`, `{ site }` from `@/data/site`, `{ SectionShell }`, `{ Reveal }`. Export `function HowItWorks()`.

Render `<SectionShell id={site.anchors.howItWorks}>` with:
- `<h2>` "How a fight works" (display font, --ufa-type-h2).
- A responsive grid (CSS grid via inline style: `gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))"`, `gap: "1.5rem"`) mapping `howItWorks` to step cards. Each card: a large step number (`step.step`) in accent color (var(--ufa-accent)), `step.title` (display font, ~1.25rem), `step.body` (muted, --ufa-type-body). Card background `var(--ufa-bg-elevated)`, `borderRadius: var(--ufa-radius)`, padding ~1.5rem, subtle `border: 1px solid var(--ufa-rule)`.
- Wrap EACH card in `<Reveal delay={i * 0.08}>` so steps stagger in; reduced motion shows all four immediately.

Server Component; reserve layout space via the grid so there is no CLS. No layout-shifting animation (Reveal handles transform/opacity only).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "howItWorks" components/sections/HowItWorks.tsx && grep -q "SectionShell" components/sections/HowItWorks.tsx && grep -q "Reveal" components/sections/HowItWorks.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/HowItWorks.tsx` exists, exports `HowItWorks`, maps `howItWorks` to step cards
    - Uses `SectionShell` and `Reveal`
    - Section root carries `id={site.anchors.howItWorks}`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>HowItWorks renders the 4-step match format as a responsive grid of cards from typed data, staggered via Reveal, legible with motion off.</done>
</task>

<task type="auto">
  <name>Task 3: Build Lineage section (CONCEPT-03)</name>
  <files>components/sections/Lineage.tsx</files>
  <read_first>
    - components/ui/SectionShell.tsx, components/ui/GradientText.tsx (REUSE)
    - components/motion/Reveal.tsx, components/motion/ScrollParallax.tsx (REUSE)
    - data/concept.ts (lineage: 3 nodes + lineageStatement)
    - styles/tokens.css
  </read_first>
  <action>
Create `components/sections/Lineage.tsx`. Top comment: `// Lineage section - UFC -> UFB -> UFA positioning. Data from data/concept.ts (placeholder).`

Import `{ lineage, lineageStatement }` from `@/data/concept`, `{ SectionShell }`, `{ GradientText }`, `{ Reveal }`, `{ ScrollParallax }`. Export `function Lineage()`.

Render `<SectionShell>` with:
- A horizontal (mobile: vertical-stacking via flex-wrap) chain of the 3 `lineage` nodes, each rendered as `node.node` (UFC / UFB / UFA) in large display type with an arrow glyph " -> " between them (render arrows as plain text or a styled span; on the final UFA node, wrap it in `<GradientText>` to make it the emphasis). Below each node, its `node.blurb` in muted body text.
- The `lineageStatement` ("the UFC of AI") as a prominent line beneath the chain, wrapped in `<Reveal>`.
- Wrap the chain in `<ScrollParallax range={40}>` for a subtle depth drift (transform-only; reduced motion -> plain div).

Use flex layout with `flexWrap: "wrap"`, `gap`, `alignItems: "center"`. Keep it a Server Component (Reveal/ScrollParallax imported as client children). Ensure the lineage reads as a sentence even with motion stripped.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "lineage" components/sections/Lineage.tsx && grep -q "SectionShell" components/sections/Lineage.tsx && grep -q "Reveal\|ScrollParallax" components/sections/Lineage.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/Lineage.tsx` exists, exports `Lineage`, maps `lineage` and renders `lineageStatement`
    - Uses `SectionShell`, `GradientText`, and at least one of `Reveal`/`ScrollParallax`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Lineage renders the UFC -> UFB -> UFA chain with UFA emphasized via GradientText and the "UFC of AI" statement, using Phase-1 motion primitives, legible with motion off.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- All three section files import from `@/data/concept` and reuse `SectionShell` + Reveal/ScrollParallax (grep)
- No animation reimplemented (no direct `motion/react` imports in section files; grep `motion/react` returns nothing in components/sections/)
- Concept + Lineage carry the credits/money + lineage message in text (data-driven), readable with motion stripped
</verification>

<success_criteria>
A visitor can read what UFA is, understand how a fight works in 4 steps, and grasp the UFC -> UFB -> UFA lineage - all from typed placeholder data, all reusing Phase-1 primitives, all fully legible with motion disabled. Sections are ready for Plan 05 to assemble into the page.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-conversion-spine/02-02-SUMMARY.md`
</output>
